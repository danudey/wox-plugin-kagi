[project]
name = "wox-plugin-kagi"
version = "1.0.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "wox-plugin==0.0.86",
]

[project.optional-dependencies]
dev = ["ruff", "mypy"]

[tool.ruff]
line-length = 140
